global:
  resolve_timeout: 5m

route:
  group_by: [alertname, service, severity]
  group_wait: 10s
  group_interval: 2m
  repeat_interval: 4h
  receiver: local-alerts

receivers:
  - name: local-alerts
